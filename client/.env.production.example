# Production Environment for Vite Frontend
# ==========================================
# This file is used when running `npm run build`
# Copy to client/.env.production and fill in values

# API Base URL - The full URL to your Django API
# In production, the API is proxied through OpenLiteSpeed at /api
VITE_API_BASE_URL=https://finance.mstatilitechnologies.com/api

# Google OAuth Client ID (same as in Django .env)
# Get this from Google Cloud Console
VITE_GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
